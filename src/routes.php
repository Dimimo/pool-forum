<?php

use Illuminate\Support\Facades\Route;
use Dimimo\PoolForum\Http\Controllers\SettingController;

// Route::prefix(config('pool-forum.prefix-route'))->middleware(config('pool-forum.middlewares'))->group(function () {
//     Route::resource('settings', SettingController::class);
// });
